# @package _global_
# Scaling study with parameter sweeps

defaults:
  - override /setting: balls_and_urns
  - override /model: llm
  - override /eval: small_lm_eval
  - _self_

# Base experiment configuration
name: llm-exp
output_dir: ${run_dir}

# Define the sweep parameters for Hydra
hydra:
  sweeper:
    params:
      model_kwargs.model_path: google/gemma-2-2b