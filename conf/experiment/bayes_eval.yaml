# @package _global_
# Scaling study with parameter sweeps

defaults:
  - override /setting: balls_and_urns
  - override /model: bayesian_model
  - override /training: default
  - _self_

# Base experiment configuration
name: bayes-eval
output_dir: ${run_dir}


# Define the sweep parameters for Hydra
hydra:
  sweeper:
    params:
      model_kwargs.model_type: bayesian_unigram